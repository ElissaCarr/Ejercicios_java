
/*package array_de_una_dimension;

public class Ejercicio4POO {

  import java.util.Random;


  public static void main(String[] args) {
    Random rand = new Random();
    int[] numero = new int[20];
    int[] cuadrado = new int[20];
    int[] cubo = new int[20];
    
    // Carga el array "numero" con valores aleatorios entre 0 y 100
    for (int i = 0; i < numero.length; i++) {
      numero[i] = rand.nextInt(101);
    }
    
    // Calcula los cuadrados y los cubos y los almacena en los arrays correspondientes
    for (int i = 0; i < numero.length; i++) {
      cuadrado[i] = numero[i] * numero[i];
      cubo[i] = numero[i] * numero[i] * numero[i];
    }
    
    // Muestra el contenido de los tres arrays dispuesto en tres columnas
    System.out.println("Numero   Cuadrado   Cubo");
    for (int i = 0; i < numero.length; i++) {
      System.out.printf("%6d   %8d   %8d%n", numero[i], cuadrado[i], cubo[i]);
    }
  }
}*/